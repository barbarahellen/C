#include<stdio.h>
// Arithmetic Operators

int main(void){

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)       rest of division
    // ++ increment
    // -- decrement
 
    int x = 5;
    int y = 2;
    
    

    int z = x + y;
    // int z = x - y;
    // int z = x * y;
    // float z = x / (float) y;
    // int z = x % y;
    // x++;
    // y--;

    printf("%d\n", x);
    printf("%d\n", y);
    printf("result: %d\n", z);

    return 0;
}